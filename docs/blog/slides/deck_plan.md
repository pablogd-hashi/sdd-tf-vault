# Deck plan

- **Mode:** reveal (exact copy for Medium; no full-slide image generation)
- **Audience:** readers of the Vault onboarding factory follow-up
- **Thesis:** Era two is you in the chair, skill by skill. Era three is an async Cloud Agent running the whole SDLC; you come back to a draft PR.
- **Asset policy:** none — HTML/CSS owns every word.

## Slides

1. **SDLC eras grid** — claim: Tab, the synchronous copilot, and the async factory change different stages; deploy stays a human merge; operate is where the factory adds Grafana and evals.
2. **How / why** — claim: the factory is a Linear status plus `AGENTS.md`, not a longer chat; context lives in the ticket and the repo.

No on-slide titles (those belonged to the talk deck). Light theme so the diagrams sit in a Medium post.
